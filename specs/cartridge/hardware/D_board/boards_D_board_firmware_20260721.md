# D基板（AVR64DD28）ファームウェア仕様書

**Rev. 2.0 — 2026年7月21日**

---

## 目次

1. [概要](#1-概要)
2. [動作モード](#2-動作モード)
3. [I2Cスレーブ動作](#3-i2cスレーブ動作)
4. [ジョイスティック入力処理](#4-ジョイスティック入力処理)
5. [OLED描画](#5-oled描画)
   - 5.1 動作モード別表示内容
   - 5.2 バッテリーデータの取得フロー
6. [スリープ制御](#6-スリープ制御)
7. [メモリマップ](#7-メモリマップ)
8. [フラッシュ寿命試算](#8-フラッシュ寿命試算)
9. [実装ロードマップ](#9-実装ロードマップ)
10. [変更履歴](#10-変更履歴)

---

## 1. 概要

本書はD基板搭載AVR64DD28のファームウェア仕様を定める。AVR64DD28はUIAPduino（CH32V003）のI2Cスレーブとして動作し、ジョイスティック入力・OLED描画・スリープ管理を担う。日本語入力システムおよびシステムメニューを含むシステム機能全般との協調動作の詳細は `CAR-01_japanese_input_20260717.md` および `features_system_menu_20260725.md` を参照。

---

## 2. 動作モード

| モード | 条件 | 動作 |
|---|---|---|
| RAWデータモード | 通常時 | ジョイスティック状態を1バイトビットフィールドでI2C Readに即答 |
| **システムメニューモード** | **ジョイスティックセンター長押し（約1秒）検知時** | **OLEDにシステムメニューUI表示・操作結果をSYS_CMDでUIAPduinoへ通知** |
| 日本語入力モード | UIAPduinoから開始通知を受信中 | ひらがな入力UI・OLED候補表示・ユーザ確定操作を全担当 |
| スリープ | UIAPduinoからスリープ指示を受信 | OLED表示オフ・定期スリープサイクル |

**モード排他制御：** 日本語入力モード中はシステムメニューモードへの遷移を禁止する（逆も同様）。AVR64DD28側でモード状態フラグを管理する。

---

## 3. I2Cスレーブ動作

UIAPduino（CH32V003）をマスターとする共通I2Cバス上でスレーブとして動作する。

| コマンド種別 | 方向 | 内容 |
|---|---|---|
| Read（RAWモード） | AVR64DD28→UIAPduino | ジョイスティック状態1バイト（bit7=0） |
| **Write/Read（システムコマンド）** | **AVR64DD28→UIAPduino** | **SYS_CMDバイト（bit7=1）：メニュー開閉・音量変更等。詳細はfeatures_system_menu_20260725.md参照** |
| Write（モード通知） | UIAPduino→AVR64DD28 | 日本語入力モード開始・終了・スリープ指示 |
| Write（バッテリー残量） | UIAPduino→AVR64DD28 | UIAPduinoがATtiny202から取得した残量値を転送 |

I2Cスレーブアドレス：TBD（PENDING #1 / `boards/D_board/hardware.md` 参照）

---

## 4. ジョイスティック入力処理

SKRHAAE010はデジタルスイッチのためADC不要。ポーリングで読み取る。

> 注: 下記コードのポート/ピン（`PORTA`・`PINn_bm`）は旧ATtiny1604割当に基づく**暫定**であり、AVR64DD28の物理ピン割当が
> 確定（HW仕様書 §4.2 PENDING #7・ユーザー決定）した時点で対応するポート/ビットへ更新する。ロジック（5入力・LOW=押下）は不変。

```c
uint8_t joystick_read(void) {
    uint8_t state = 0;
    if (!(PORTA.IN & PIN5_bm)) state |= JOY_UP;
    if (!(PORTA.IN & PIN6_bm)) state |= JOY_DOWN;
    if (!(PORTA.IN & PIN4_bm)) state |= JOY_LEFT;
    if (!(PORTA.IN & PIN3_bm)) state |= JOY_RIGHT;
    if (!(PORTA.IN & PIN7_bm)) state |= JOY_CENTER;
    return state;
}
```

**チャタリング除去：** 同一状態が連続2回（約10ms間隔）確認された時点で確定。

---

## 5. OLED描画

CH1115（I2Cアドレス0x3C）への描画はAVR64DD28が単独で担当する。UIAPduinoはOLEDに直接書き込まない。

**4階調表示**: CH1115は物理的に2値だが、AVR64DD28が4階調に対応する複数プレーンを**タイマ割込で高速にI2C送出する時間的
ディザリング**で4階調（2ビット）を再現する。フォントは主基板TFTと同一の2ビット4階調規格（HW仕様書 §4.4／§4.5・8ドット/12ドット
2サイズをローカルFlashに保持）。AVR64DD28はDMA非搭載のため、プレーン送出はタイマ割込＋ポーリングで組む（8KB SRAMにプレーンバッファを確保）。

### 5.1 動作モード別表示内容

| モード | OLED表示内容 |
|---|---|
| 日本語入力モード | 確定文字列・変換対象ひらがな・候補文字列群・バッテリー残量 |
| 通常モード | バッテリー残量＋スクリーンセーバー等の自由演出 |
| スリープ | OLED表示オフ |

### 5.2 バッテリーデータの取得フロー

AVR64DD28はバッテリー残量データをUIAPduino経由で受け取る。ATtiny202に直接アクセスしない。

```
UIAPduino → ATtiny202へReadリクエスト（定期ポーリング）
         ← 残量値1バイトを受信
UIAPduino → AVR64DD28へWrite（残量値を転送）
AVR64DD28 → 次回OLED描画時にバッテリー表示に反映
```

---

## 6. スリープ制御

```
[通常時]
AVR64DD28: 定期スリープ（数秒おき）
            ウェイクアップ → バッテリー表示更新 → スリープ

[日本語入力モード開始通知受信時]
AVR64DD28: スリープ禁止・フルスピード動作
            ジョイスティック入力監視・OLED描画・候補表示

[日本語入力モード終了通知受信時]
AVR64DD28: 通常時スリープサイクルへ復帰
```

---

## 7. メモリマップ

> ATtiny1604（16KB Flash / 1KB SRAM）からAVR64DD28（**64KB Flash / 8KB SRAM**）への移行に伴い再構成した。
> **具体アドレスは暫定**（ファーム実装時に確定・物理ピン割当PENDING #7と併せて詰める）。

| 領域 | サイズ（目安） | 内容 |
|---|---|---|
| ローカルフォント（2ビット4階調・2サイズ） | 約14.2KB | 8ドット16B/字＋12ドット36B/字・入力サブセット約280字×2サイズ（HW仕様書§4.5参照）。旧2KB・256文字1ビットから拡大 |
| プログラム領域 | 十数KB | I2C制御・ジョイスティック・OLED描画・**時間的ディザリング制御**・日本語入力UI・ウェアレベリング等 |
| 候補データ動的バッファ | 8KB程度 | 64B/ページ単位・自己書き換え（ウェアレベリングで分散） |
| 予備／将来拡張 | 残り | 64KB化で確保した余裕 |
| 内蔵EEPROM | 256B（AVR64DD28） | ウェアレベリングカウンタ等 |
| 内蔵SRAM | 8KB | UI操作・通信ワークエリア・**ディザリング用プレーンバッファ**（DMA非搭載のため割込＋ポーリングで送出） |

---

## 8. フラッシュ寿命試算

候補データバッファ（8KB・128ページ）への書き込みを完全巡回ラウンドロビンで分散。

| 想定使用頻度 | 推定寿命 |
|---|---|
| 1日20回変換（ヘビーユーズ） | 約22年 |
| EEPROMの同時消耗 | フラッシュ寿命時点でEEPROM定格の10%のみ消費 |
| チップ交換コスト | AVR64DD28（ATtiny1604より高価だが数百円級）・PCBA実装。候補バッファのウェアレベリングは64KB化で更に余裕 |

---

## 8.5 【要件・2026-08-07 追加／2026-08-08 改訂】主基板の異常時に復帰を誘導する

**状態: 要件として起票。設計・実装は未着手。**
主基板側の方式は 2026-08-08 に確定した（主基板リポジトリ `docs/decisions/0003-failstop-recovery-via-dboard.md`）。

### 背景

主基板（UIAPduino）には `for(;;)` による fail-closed 停止が**38箇所**ある。
実機では独立ウォッチドッグ（IWDG・約8秒）が働くため、停止すると**自動的に再起動**する。
このとき主基板は SRAM を失い、原因を自分で覚えていられない。

一方 **D基板は主基板と一緒にはリセットされない**（電源は A基板供給・主基板からのリセット線を
設けない要件＝ハード仕様 §6.1）。∴ **D基板が異常の記憶を担える。**

### 要件

| # | 内容 |
|---|---|
| 1 | 主基板から I2C で受け取った**エラーコード（`REG_ALERT`）を保持する**。主基板がリセットしても失わないこと |
| 2 | コードに応じた**メッセージを OLED に表示**し、利用者の確認操作（OK）を待つ |
| 3 | 確認後、主基板の起動シーケンスに対して**キー入力を返す**ことで、主基板をアプリ選択画面へ誘導する ★**2026-08-08 緩和**: 主基板は IWDG リセット後に**自分でアプリ選択画面へ行く**ことが判明した（後述）。∴ 本要件は**必須ではない**。返すなら `BTN_OK` 相当でよく、「同時押し」の新しい表現を定義する必要はない |
| 4 | 誘導が完了したらエラーコードを消去し、通常のキー入力に戻る |
| **5** | **主基板からの I2C トランザクションが一定時間途絶えたこと（沈黙）を検知し、エラーコードを受け取っていなくても復帰誘導に入る**（2026-08-08 追加） |
| **6** | **要件5 の検知は I2C 割込みではなく独立したタイマで行うこと**（2026-08-08 追加） |

### ★要件5・6 を足した理由（2026-08-08）

**主基板は、コードを送れないまま止まりうる。**
`send_dboard_alert()` の下にある I2C 待ちループにはタイムアウトが無く
（主基板 `i2c_comms.c:82 / :106 / :108`）、バスが固まると**コードを送る前に止まる**。
このとき要件1〜4 はすべて前提が崩れる——**どれも「コードを受け取った」ことから始まっているため。**

しかし **D基板は主基板が黙ったこと自体を検知できる**:

- 主基板は D基板の `REG_INPUT` を **約16ms周期（≈60Hz）でポーリングしている**
  （主基板 `i2c_comms.c` の `SVC_PUMP_TICKS = Ticks_from_Ms(16)`）
- 主基板が停止すればこれが途絶える
- **主基板のアプリはウォッチドッグを自力でクリアできない**（`rtc_system_tick()` は主基板の
  内部ヘッダにあり RC_API に公開されていない）。∴ 主基板が8秒以上生き続けるには必ず
  common_prog の経路を通る必要があり、その経路は I2C も回している
  （`keyscan_wait` は 1ms ループで WDT クリアと入力ポーリングを同じループで叩く。
  Flash 書込 614〜794ms も 256B スライス境界で明示的に入力ポーリングを呼ぶ）

∴ **沈黙検知があれば、コード送出の成否によらず復帰誘導を打てる。**
主基板側に I2C タイムアウトを実装する必要は無い（1層 FLASH を払っても利用者に見える差が無い）。

| | D基板の状態 | 表示 | 復帰誘導 |
|---|---|---|---|
| コードが届いた | コード＋沈黙 | 対処法別の具体文面（後述 P1〜P4） | 同時押し合成 |
| **コードが届かない** | **沈黙のみ** | **「本体が応答しません」＝実質 P4** | **同時押し合成（同じ）** |

**要件6（独立タイマ）の理由**: 主基板がトランザクションの途中で停止し SCL を Low に握った場合、
D基板の I2C 周辺は先へ進まない。**I2C 割込みを起点にすると検知自体が道連れになる。**
時間軸は I2C から独立していなければならない（AVR64DD28 の TCB 等）。

### ★要件3が必須でない理由（2026-08-08 判明）

**主基板は IWDG リセット由来かを自分で判定し、その場合は自動起動をしない。**

- `common_prog.c:41` がリセット直後に `RCC->RSTSCKR` の `IWDGRSTF` を採取する
  （次のリセットで上書きされるため最先頭で読む必要がある）
- `boot.c:231` の `if (!btn_ok && !iwdg_reset)` により、**IWDG リセット後は
  App Area の自動起動を飛ばしてアプリ選択画面へ行く**

∴ 主基板が fail-closed で停止 → IWDG が約8秒でリセット → 再起動 →
**D基板が何もしなくてもアプリ選択画面が出る。**

D基板の役割は「**利用者に何が起きたか伝え、どうすればよいか示す**」ことであり、
画面遷移そのものを起こす必要はない。要件3は「OK 押下を待ってから進ませたい」という
UX 上の理由でのみ意味を持つ（押されるまで主基板を待たせる手段は別途要検討）。

★**主基板側にはこの経路を踏む試験が現状1件も無い**（2026-08-08 確認）。
D基板側の実装に着手する前に、主基板側で検証されている必要がある。

### ★エラーコードは「対処法」で分類する（2026-08-08 確定）

**利用者が知りたいのはエラーコードではなく「どうしたらよいか」である。**
検知点の条件文をそのまま表示しても行動につながらない。
主基板側で数え直した結果、**利用者に開かれた行動は4通りしかない**。

| | 対処法 | OLED に出す内容 | 主基板側コード |
|---|---|---|---|
| **P1** | そのまま続けられる | 「一時的な異常が起きました。アプリを選び直してください。繰り返す場合は本体の不具合です」 | `0x01` |
| **P2** | そのアプリだけ使えない | 「アプリ〈X〉の作業データが壊れています。別のアプリを選び、システムメニューから〈X〉を終了してください。**セーブデータは残ります**」 | `0x02` |
| **P3** | カートリッジを書き直す | 「カートリッジのデータを読み込めません。PC に接続して書き込み直してください」 | `0x03` |
| **P3'** | （アプリ未投入） | 「アプリが入っていません。PC に接続して書き込んでください」 | `0x04` |
| **P4** | 利用者にできることはない | 「本体に異常があります。コード **XX** を控えてお問い合わせください」 | `0x05`〜`0x14`（16種） |
| **P4'** | （沈黙検知・コード未受信） | 「本体が応答しません。再起動します」 | — |

★**コードを利用者に見せるのは P4 だけ。** P1〜P3 は文面で行動が完結する。
★P1〜P4 は**フォールバックの鎖**（前が効かなければ次へ）。文面にその誘導を含めること。
★主基板側のコード対応表の正本＝主基板リポジトリ `docs/open_issue_failstop_and_wdt.md`。

### なぜ D基板側でやるのか

主基板の 1層 FLASH は **5632B の枠に対し現在 6524B（+892B 超過）** で、余地が無い。
主基板側で異常フラグを FRAM に永続化し起動時に読む方式だと、その分の実装が主基板に載る。
**D基板に状態を持たせれば、主基板側の追加はエラーコードの割り当てと入力分岐だけで済む。**

### 未確定事項

- **沈黙検知のしきい値。** 正常時は約16ms周期、8秒以上の沈黙は主基板の生存と両立しない、
  までは分かっている。**その間の実際の最長ギャップ（Flash 書込中・全画面合成中）は未測定。**
  1秒級のしきい値を置くなら測ってから決めること
- ~~「同時押し」に何を使うか~~ → **解消**。主基板の起動時ボタン確認は `BTN_OK` を見ており
  （`boot.c:229`）、`BTN_OK` を返せばアプリ選択画面へ入る。新しい表現は不要
- **表示メッセージの最終文言**（上表は案。48×88 の OLED に収まるかは要検討）
- **P2 の〈X〉にアプリ名を出せるか。** 主基板は1バイトのコードしか送らないため、
  D基板がどのアプリだったかを知る手段が現状無い。アプリ名を出さない文面にするか、
  別途アプリ ID を送るかは未決定

---

## 9. 実装ロードマップ

| Phase | 内容 | 成果物 |
|---|---|---|
| Phase 2 | AVR64DD28ファームウェア RAWモード・ひらがな入力・OLED描画（4階調ディザリング）・ウェアレベリング | avr64dd28_fw.hex |
| Phase 5 | 呼び出し元アプリ連携試験 | 結合動作確認 |

※Phase 1・3・4はUIAPduino側の実装。`CAR-01_japanese_input_20260717.md` 参照。

---

## 10. 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-08-07 | §8.5 を追加。**主基板の異常時にエラーコードを保持し、再起動後にキー入力を返して復帰を誘導する**要件を起票（主基板の内部リセットは D基板に及ばないことを RM V1.9 §3.2 Figure 3-1 で確認済み）。設計・実装は未着手。 |
| 2026-07-21 | Rev.2.0 **MCUをATtiny1604→AVR64DD28へグレードアップ**（64KB Flash / 8KB SRAM / MVIO / DMA非搭載）。タイトル・§1概要・全体のMCU名を更新。§5 OLED描画に**4階調時間的ディザリング**を追記（複数プレーンをタイマ割込でI2C送出・主基板と同一2ビット4階調規格）。§7メモリマップを64KB/8KB前提へ再構成（**ローカルフォント2ビット4階調2サイズ約14.2KB**・ディザリング用プレーンバッファ・具体アドレスは暫定）。§4ジョイスティックのポート/ピンは暫定（AVR64DD28物理ピン割当=HW §4.2 PENDING #7確定後に更新）。§8寿命試算・§9ロードマップ（avr64dd28_fw.hex）を更新。 |
| 2026-06-10 | Rev.1.0 新規作成。latcaputa_wnn_prospectus_rev2.docx（Rev.2.0）およびD_board_spec Rev.3.1のファームウェア部分を統合・独立ファイル化 |
| 2026-06-10 | §7 メモリマップのプログラム領域説明からセマフォを削除（Rev.3.0でセマフォ共有方式廃止・ATtiny1604単独管理に変更済みのため） |
| 2026-06-12 | Rev.1.1。§1 概要にsystem_menu.md参照を追加。§2 動作モードにシステムメニューモードを追加・モード排他制御を明記。§3 I2CスレーブコマンドにSYS_CMD（bit7=1）を追加。目次を追加。 |
