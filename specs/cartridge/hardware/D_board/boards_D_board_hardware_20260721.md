# D基板（日本語入力フロントエンド）ハードウェア仕様書

**Rev. 2.0 — 2026年7月21日**
**対象: CAR-01（UIAPduino版・FRISK筐体）**

---

## 目次

1. [本書の位置づけ](#1-本書の位置づけ)
2. [概要・目的](#2-概要目的)
3. [物理仕様](#3-物理仕様)
4. [回路設計](#4-回路設計)
   - 4.1 搭載部品
   - 4.2 ピンアサイン（AVR64DD28）
   - 4.3 ジョイスティック仕様
   - 4.4 4階調時間的ディザリング表示
   - 4.5 ローカルフォント（2ビット4階調・2サイズ）
5. [部品一覧（BOM）](#5-部品一覧bom)
6. [外部接続・インターフェース](#6-外部接続インターフェース)
   - 6.1 エッジパッド一覧
   - 6.2 OLED仕様
7. [Claude Code用 回路図自動生成指示書](#7-claude-code用-回路図自動生成指示書)
8. [設計根拠メモ](#8-設計根拠メモ)
9. [未決事項（PENDING）](#9-未決事項pending)
10. [変更履歴](#10-変更履歴)

---

## 1. 本書の位置づけ

本書はラテカピュータ（CAR-01規格）のD基板（日本語入力フロントエンド）のハードウェア仕様を定める。ファームウェア仕様は `boards_D_board_firmware_20260612.md` を参照。

**参照ドキュメント：**

| ドキュメント | 参照事項 |
|---|---|
| cartridge_master_20260612.md | CAR-01全体規格・I2Cバストポロジー |
| boards_A_board_hardware_20260612.md | VCC_3V3_OUT供給元・電源シーケンス |
| boards_D_board_firmware_20260721.md | AVR64DD28ファームウェア仕様・動作モード |
| CAR-01_japanese_input_20260717.md | 日本語入力サブシステム仕様 |
| features_system_menu_20260725.md | システムメニュー・音量調整・ジョイスティック操作定義 |

---

## 2. 概要・目的

D基板はラテカピュータ（CAR-01規格）の日本語入力フロントエンドおよびシステムUI操作を担う専用基板である。AVR64DD28・CH1115 OLED・SKRHAAEジョイスティックの全機能をこの1枚に集約する。

ジョイスティック入力の監視・OLED描画（4階調ディザリング）・日本語入力UIをAVR64DD28が完結させ、UIAPduino（CH32V003）のCPU負荷を最小化する。日本語入力システムの詳細は `CAR-01_japanese_input_20260717.md` を、システムメニュー操作の詳細は `features_system_menu_20260725.md` を参照。

---

## 3. 物理仕様

| 項目 | 仕様 |
|---|---|
| 基板サイズ | 19mm × 34mm（縦34mmはA基板・C基板とパネライズグリッドを統一） |
| 基板厚 | 0.8mm（JLCPCB 2層 FR-4） |
| 実装方式 | 片面実装 |
| パネライズグリッド | 19mm幅（B基板と同幅）・34mm縦（A基板・C基板と統一） |

**ケース内配置方針：**
SKRHAAE010のDIP変換キット（2.54mmピッチ）の実装面積が支配的。ジョイスティック操作面をケース窓から露出させる位置に配置する。OLEDもケース表面の窓から露出させる。

---

## 4. 回路設計

### 4.1 搭載部品

| 部品 | 型番 | 仕様 | 備考 |
|---|---|---|---|
| MCU | AVR64DD28 | AVR DD-Series・28ピン・64KB Flash・8KB SRAM・最大24MHz・MVIO・**DMA非搭載** | I2Cクライアント（共通バス・スレーブ）＋I2Cホスト（CH1115ローカル線）＝**TWI Dual Mode**。OLED描画（4階調ディザリング）＋ジョイスティック制御＋ローカルフォント格納 |
| OLED | CH1115 | 0.5インチ・48×88ドット・I2C（0x3C） | **AVR64DD28のhostローカル専用線（PA2/PA3・Fm+ 1MHz）に接続**（共通バス非接続）・描画はAVR64DD28が単独担当（4階調は時間的ディザリング） |
| ジョイスティック | Alps Alpine SKRHAAE010（秋月 AE-SKRHAAE010-BO） | 4方向＋センタープッシュ・デジタル5ポジション | プルアップ抵抗実装済みDIP変換キット |

### 4.2 ピンアサイン（AVR64DD28・**物理ピン割当 ✅確定**）

> **【PENDING #7 解決・2026-07-24】** AVR64DD28（28ピン）の物理ピン割当をユーザーが確定した。ジョイスティック・各ボタン
> 端子を基板の片側へ寄せる物理配置（§8）を優先しつつ、**I2Cは TWI Dual Mode（`TWIROUTEA=0x0`）** で構成する
> ＝**host=PA2/PA3 → CH1115（D基板ローカル専用線・Fm+ 1MHz）／client=PC2/PC3 → 共通I2Cバス（400kHz・マスターは主基板）**。
> 確定の導出過程は目論見書 `dboard_role_and_protocol_prospectus_20260724.md` §1.1 を参照。

**物理ピン割当（28ピン全体）：**

| # | ピン | 論理信号 | 方向 | 接続先・備考 |
|---|---|---|---|---|
| 1 | PA7 | JOY_CENTER | IN | SKRHAAE010 センタープッシュ |
| 2 | PC0 | JOY_DOWN | IN | SKRHAAE010 下 |
| 3 | PC1 | JOY_LEFT | IN | SKRHAAE010 左 |
| 4 | PC2 | SDA (client) | INOUT | 共通I2Cバス・スレーブ側（マスター=主基板・400kHz） |
| 5 | PC3 | SCL (client) | INOUT | 共通I2Cバス・スレーブ側 |
| 6 | VDD2 | （電源） | — | 未使用 |
| 7 | PD1 | JOY_RIGHT | IN | SKRHAAE010 右 |
| 8 | PD2 | EVOUTD | — | 将来イベント出力枠（空き） |
| 9 | PD3 | JOY_UP | IN | SKRHAAE010 上 |
| 10 | PD4 | BTN_X | IN | Xボタン（プルアップ・LOW=押下） |
| 11 | PD5 | BTN_Y | IN | Yボタン |
| 12 | PD6 | BTN_A | IN | Aボタン |
| 13 | PD7 | BTN_B | IN | Bボタン |
| 14 | VDD | （電源） | — | |
| 15 | GND | （電源） | — | |
| 16 | PF0 | BTN_MENU/CANCEL | IN | メニュー/キャンセルボタン |
| 17 | PF1 | （予備） | — | 空き・将来拡張用 |
| 18 | PF6 | RESET | IN | リセット |
| 19 | PF7 | UPDI (LOCAL_UPDI_D) | INOUT | 書込み・UIAPduino PD0へ接続（セルフアップデート用） |
| 20 | VDD | （電源） | — | |
| 21 | GND | （電源） | — | |
| 22 | PA0 | （予備） | — | 空き・将来拡張用 |
| 23 | PA1 | SPI0_CS | OUT | SPI0スレーブ選択（将来枠） |
| 24 | PA2 | SDA (host) | INOUT | CH1115ローカル専用線（Fm+ 1MHz） |
| 25 | PA3 | SCL (host) | INOUT | CH1115ローカル専用線（Fm+ 1MHz） |
| 26 | PA4 | SPI0_MOSI | OUT | SPI0（将来枠） |
| 27 | PA5 | SPI0_MISO | IN | SPI0（将来枠） |
| 28 | PA6 | SPI0_SCK | OUT | SPI0（将来枠） |

> 電源: VCC_3.3V=A基板 Pad3（VCC_3V3_OUT）。VDD2(6番)は未使用（Fm+のVDD≥2.7V条件は§9で追跡）。

**LOCAL_UPDI_Dの兼用制約：**
UIAPduino PD0はe-ink版ではBUSY_EINK、TFT版ではGPIO4_CSとしても使用する。AVR64DD28アップデート中はe-ink描画コマンドを送らないこと。TFT版は拡張デバイスのCSをネゲートすること。UPDIは1対1接続専用。ATtiny202（A基板）はUIAPduino PD6（LOCAL_UPDI_A）で別途接続。同時アップデート不可。

### 4.3 ジョイスティック仕様

**Alps Alpine SKRHAAE010（秋月電子 AE-SKRHAAE010-BO）**

| 項目 | 仕様 |
|---|---|
| 方式 | デジタル5ポジションスイッチ（アナログ軸なし） |
| 方向数 | 4方向（上・下・左・右）＋センタープッシュ |
| 接点形式 | スイッチ接点（プルアップ時・押下でLOW） |
| 定格 | 50mA / 12V DC |
| 外形 | 7.35×7.5×1.8mm（SMD） |
| 動作寿命 | 200,000サイクル |
| キット付属 | 10kΩプルアップ抵抗実装済み・2.54mmピッチDIP変換基板 |

**ジョイスティック入力ビットフィールド（I2C返答フォーマット・1バイト）：**

```c
#define JOY_UP      0x01  // PD3 LOW
#define JOY_DOWN    0x02  // PC0 LOW
#define JOY_LEFT    0x04  // PC1 LOW
#define JOY_RIGHT   0x08  // PD1 LOW
#define JOY_CENTER  0x10  // PA7 LOW
// bit5〜bit6: 予備（将来拡張）
// bit7: 1=システムコマンド（SYS_CMD）・0=RAWデータ
```

### 4.4 4階調時間的ディザリング表示

CH1115は物理的に2値（点灯/消灯）OLEDだが、**4階調（2ビット）を時間的ディザリングで再現する**。4階調に対応する
複数プレーンをタイマ割込で高速に切り替えてI2C送出し、残像効果で中間調を見せる。フォント・描画データは
**主基板TFTと同一の2ビット4階調規格**を共有する（同一アセットを主基板・D基板・入力フォントで統一）。

- **AVR64DD28はDMA非搭載**（AVR-Dx系はDMAコントローラを持たない）。プレーン送出のI2C転送は**タイマ割込＋ポーリング**で
  組む。8KB SRAM・最大24MHzのリソース増により、ディザリングのプレーン保持と定周期送出のオーバーヘッドを吸収できる
  （ATtiny1604の1KB SRAM・16KB Flashでは余地が無かった＝MCUグレードアップの主目的の一つ）。

### 4.5 ローカルフォント（2ビット4階調・2サイズ）

D基板は入力UI用フォントを**ローカルFlashに保持**し、UIAPduino/SPI Flashへの往復なしに描画を完結させる。

| サイズ | 1文字あたり | 用途 |
|---|---|---|
| 小（8ドット） | 16B（2ビット4階調） | 候補文字列・選択文字群 |
| 大（12ドット） | 36B（2ビット4階調） | 最上位入力・変換中文字列 |

- 文字集合＝**入力サブセット約280字**（全角 英数/かな/カナ/記号）。2サイズ合計 = **約14.2KB（64KB Flashの約22%）**。
  FEP制御・辞書サブセットを含めても余裕がある。
- **混在レイアウト**（最上位=12ドット・候補=8ドット）を同一画面で同時に扱うため2サイズをローカル保持し、D基板単独で
  ゼロ遅延に描画する。主基板SPI Flashのフォントと**同一2ビット規格**（統一インデックス空間の部分集合）。

---

## 5. 部品一覧（BOM）

### 搭載部品一覧（実装方法別）

| RefDes | 部品名・仕様 | 型番 | 実装方法 | 入手先 |
|---|---|---|---|---|
| U1 | MCU AVR64DD28 | AVR64DD28（パッケージ・LCSC番号は要確定・PENDING #5） | JLCPCB PCBA | LCSC |
| U2 | OLED CH1115 | CH1115 0.5インチ 48×88 | 手はんだ | 秋月電子等 |
| J1 | ジョイスティック SKRHAAE010 | AE-SKRHAAE010-BO | 手はんだ（DIP変換キット） | 秋月電子 |

> **注：** D基板は部品点数が少なく、受動部品（プルアップ抵抗等）はジョイスティックキットに付属。JLCPCB PCBA対象部品の確定はPENDING #5を参照。

---

## 6. 外部接続・インターフェース

### 6.1 エッジパッド一覧

D基板のエッジパッド構成は基板サイズ確定後に正式決定する（PENDING #2）。現時点での想定は以下の通り。

> ### ★要件（2026-08-07 追加・PENDING #2 の確定時に必ず満たすこと）
>
> **主基板（UIAPduino）のリセットが D基板に及んではならない。**
> 主基板からのリセット線をエッジパッドに設けないこと。電源も主基板からは取らない
> （現行どおり A基板 `VCC_3V3_OUT` から供給する）。
>
> **なぜ**: 主基板が異常停止した際、エラーコードを D基板へ預け、
> 主基板の再起動後に D基板がそれを主基板へ返す（＝復帰誘導）という設計が検討されている。
> **D基板が主基板と一緒にリセットされると、この経路が成立しない。**
>
> **成立の根拠（確認済み）**: CH32V003 の内部リセット（ソフトウェアリセット・IWDG）は
> NRST ピンへ出力されない。Reference Manual V1.9 §3.2 Figure 3-1 で NRST は
> 他の内部リセット源と**並列に System Reset へ入る片方向の入力**であり、逆向きの経路は無い。
> ∴ 配線でリセット線を足さない限り、主基板のリセットは D基板に伝わらない。


| 信号名 | 方向 | 備考 |
|---|---|---|
| VCC_3.3V | 受信 | A基板 Pad3（VCC_3V3_OUT）から供給 |
| GND | 共通 | |
| SDA | 双方向 | 共通I2Cバス・**クライアント（スレーブ）側**（AVR64DD28 PC2）。マスターは主基板。UIAPduino・ATtiny202と共有 |
| SCL | 双方向 | 共通I2Cバス・**クライアント（スレーブ）側**（AVR64DD28 PC3） |
| LOCAL_UPDI_D | 双方向 | UIAPduino PD0へ接続・セルフアップデート用 |
| RESET | 受信 | AVR64DD28 RESET（PF6） |
| MENU/CANCEL | 受信 | メニュー/キャンセルボタン入力（PF0） |
| SCK | 出力 | SPI0（将来枠・PA6） |
| MISO | 受信 | SPI0（将来枠・PA5） |
| MOSI | 出力 | SPI0（将来枠・PA4） |
| CS | 出力 | SPI0スレーブ選択（将来枠・PA1） |

### 6.2 OLED仕様（CH1115）

CH1115（0.5インチ・48×88ドット）はD基板上に実装し、**AVR64DD28の host ローカル専用線（PA2=SDA / PA3=SCL・Fm+ 1MHz）** に接続する。**TWI Dual Mode により CH1115 は共通I2Cバスから分離されており、UIAPduino（主基板）からは見えない**（アドレス0x3C はローカル線上でのみ有効）。描画はAVR64DD28が単独で担当する（4階調は時間的ディザリング・§4.4）。ローカル線を1MHz（Fm+）で駆動することでちらつきを52〜70Hzへ引き上げる（DEFAULT 0x0 ルーティングでのみ Fm+ が保証される）。

**画面レイアウト（横長・高さ48×幅88ドット）：**

```
┌─────────────────────────────────────────┐  ← 上端
│  確定文字列（12ドット）                    │
├─────────────────────────────────────────┤  ← ライン（1ドット）
│  変換対象ひらがな文（9ドット）              │  区切り対象文字列は反転表示
├─────────────────────────────────────────┤  ← ライン（1ドット）
│  候補文字列群（24ドット）                  │  3行 × 8ドット
│  バッテリー残量（1ドット）                 │
└─────────────────────────────────────────┘  ← 下端
```

**バッテリー残量表示仕様：**

| 項目 | 仕様 |
|---|---|
| 1ブロックのサイズ | 幅8ドット × 高さ1ドット |
| 1ブロックの表す残量 | 10% |
| 満充電（100%）の幅 | 80ドット（8ドット × 10ブロック） |
| 表示位置 | 下端1ドット・左寄せ |
| 端数ブロックの演出 | ゆっくり点滅 |

---

## 7. Claude Code用 回路図自動生成指示書

D基板はAVR64DD28・CH1115 OLED・SKRHAAE010ジョイスティックで構成されるマイコン搭載基板であり、KiCadスケマの自動生成指示書は別途作成する。本セクションは回路図生成着手時に追記する（PENDING #6）。

---

## 8. 設計根拠メモ

### AVR64DD28採用理由（ATtiny1604からのグレードアップ）

旧仕様ではATtiny1604（tinyAVR 0-Series・14ピン・16KB Flash・1KB SRAM）を採用していたが、以下の理由でAVR64DD28
（AVR DD-Series・28ピン・64KB Flash・8KB SRAM・MVIO）へグレードアップした。

1. **物理配置（レイアウト）上の制約が決定的**: ジョイスティックと各ボタンの端子を基板の**片側へ寄せて配線する**
   必要があり、14ピンのATtiny1604では**反対側にも多くのピンを基板のへりまで引き回さねばならず、配置的に無理があった**。
   **あえてパッケージサイズが大きくなってもピン数の多いパッケージを選定**し、端子を片側に集約できるレイアウト自由度を得た。
   （＝ピン数の余裕はレイアウト最適化のためであり、全ピンを機能で使い切る意図ではない。§4.2予備ピン参照。）
2. **リソース増で4階調ディザリング＋ローカルフォント格納の余地が生まれた**: 1KB SRAM/16KB Flashではディザリングの
   プレーン保持・2サイズ4階調フォント（約14.2KB）の格納に余地が無かった。8KB SRAM・64KB Flashでこれらを収容できる（§4.4/§4.5）。
3. **DMA非搭載は許容**: AVR-Dx系はDMAコントローラを持たないが、ディザリングのプレーン送出はタイマ割込＋ポーリングで
   成立する（大きいSRAM・クロックで吸収）。「D基板がDMA本命」という当初の見立ては誤りで、DMAは不要と確定した。

なおATtiny202（A基板・2KB Flash）との使い分けは従来どおり: A基板は電源管理等の小機能、D基板はOLED描画・日本語入力UI・
システムメニューUI・フォント格納を担うため大容量MCUが必要。

### SKRHAAE010採用理由

Alps Alpine製デジタル5ポジションスイッチ。アナログ軸なしのためADC不要・デジタルGPIOのみで制御できる。秋月電子でプルアップ抵抗実装済みDIP変換キット（AE-SKRHAAE010-BO）として入手でき、実装・配線が容易。動作寿命200,000サイクルはラテカピュータの想定使用年数に対して十分。

### LOCAL_UPDI_D接続先（PD0）

旧仕様（Rev.3.1）ではUIAPduino PD6に接続していたが、PD0/PD6のUPDI割り当て入れ替えによりPD0に変更した。PD0はe-ink版ではBUSY_EINK・TFT版ではGPIO4_CSと兼用であり、AVR64DD28アップデート中はそれぞれ描画停止・CS解放が必要。

### OLEDをAVR64DD28が単独管理する理由

UIAPduino（CH32V003）のCPU負荷を最小化するため。日本語入力候補表示・システムメニューUIともにAVR64DD28側で完結させることで、UIAPduinoはFM音源・SPI Flash・ゲームロジックに専念できる。

---

## 9. 未決事項（PENDING）

| # | 項目 | 優先度 | 内容 |
|---|---|---|---|
| 1 | AVR64DD28のI2Cスレーブアドレス確定 | 高 | UIAPduino・ATtiny202（A基板）と競合しないアドレスを確定 |
| 2 | D基板の基板サイズ確定 | ✅解決 | 19×34mmに確定（A・C基板と34mmグリッド統一） |
| 3 | SKRHAAE010の実装高さとケース窓設計 | 高 | フリスクケース内収容可否の実機確認 |
| 4 | LOCAL_UPDI_DエッジパッドのD基板上配置確定 | 中 | PF7（UPDI）をエッジパッドとして引き出す位置・UIAPduino PD0との配線経路確定 |
| 5 | JLCPCB PCBA対象部品の確定 | 中 | AVR64DD28のパッケージ・LCSC番号確認・PCBA委託範囲確定 |
| 6 | Claude Code用KiCadスケマ生成指示書作成 | — | §7に追記。基板サイズ確定後に着手 |
| 7 | **AVR64DD28の物理ピン割当（§4.2）** | ✅解決 | 2026-07-24 ユーザー確定。§4.2に確定28ピンマップを反映（TWI Dual Mode・TWIROUTEA=0x0） |
| 8 | Fm+（1MHz）の VDD≥2.7V 電気条件 | 中 | CH1115 host線を Fm+ 駆動するための電源条件。D基板VDD確定時に要確認（MVIO/VDDIO2系統も） |

---

## 10. 変更履歴

| 日付 | バージョン | 内容 |
|---|---|---|
| 2026-07-24 | Rev.2.1 | **★物理ピン割当を利用者確定（PENDING #7解決）＋TWI Dual Mode を全面反映**。**§4.2**＝確定28ピンマップへ全面差し替え（host=PA2/PA3→CH1115 Fm+1MHz／client=PC2/PC3→共通バス400kHz・TWIROUTEA=0x0・ジョイスティック/X-Y-A-Bボタン/SPI0将来枠/EVOUTD/RESET/UPDIの物理ピンを確定）。**§4.3**＝ジョイスティック入力ビットフィールドのピン名を確定マップへ更新（UP=PD3/DOWN=PC0/LEFT=PC1/RIGHT=PD1/CENTER=PA7）。**§4.1**＝MCUをDual Mode（クライアント＋ホスト）と明記・CH1115諸元をhostローカル線接続へ是正。**§6.1**＝エッジパッド一覧にRESET(PF6)・MENU/CANCEL(PF0)・SPI0将来枠(SCK/MISO/MOSI=PA6/5/4・CS=PA1)を追加、SDA/SCLをクライアント（スレーブ）側と明記。**§6.2**＝画面レイアウト更新（確定文字列9→12ドット・残量上のライン削除・残量3→1ドット＝計48ドット維持）＋本文のCH1115を共通バス非接続（UIAPduino不可視）のhostローカル線へ全面是正。**§9**＝PENDING #7解決・#4のUPDIピンをPF7へ是正・#8にFm+のVDD≥2.7V電気条件を追加。 |
| 2026-07-21 | Rev.2.0 | **MCUをATtiny1604→AVR64DD28へグレードアップ**（28ピン・64KB Flash・8KB SRAM・MVIO・DMA非搭載）。§4.1諸元・§5 BOM・タイトル参照を更新。§4.2ピンアサインは物理ピン割当をPENDING #7化（論理信号のみ記載・物理ピンはユーザーがレイアウトと併せ決定）。§4.4「4階調時間的ディザリング表示」・§4.5「ローカルフォント（2ビット4階調・2サイズ・約14.2KB）」を新設。§8採用理由をAVR64DD28版へ書き換え（物理配置制約=端子片側集約のためのピン数余裕確保／リソース増でディザリング・フォント格納の余地／DMA不要確定）。ファーム仕様は boards_D_board_firmware_20260721.md。 |
| 2026-06-10 | Rev.1.0 | 新規作成。D_board_spec_20260610.md（Rev.3.1）のハードウェア部分を独立ファイル化。firmware仕様はboards_D_board_firmware.mdへ、日本語入力連携仕様はfeatures/japanese_input.mdへ分離 |
| 2026-06-18 | Rev.1.2 | 基板サイズを19×27.8mm（PENDING）→19×34mmに確定（A基板・C基板とパネライズグリッド統一）。PENDING #2を解決済みに更新 |
| 2026-06-12 | Rev.1.1 | パターンZ章立てに全面再編。§1「本書の位置づけ」新設（参照ドキュメント表追加）。§2「概要・目的」にシステムメニュー参照を追加。§3「物理仕様」に独立。§4「回路設計」に搭載部品・ピンアサイン・ジョイスティック仕様を統合。LOCAL_UPDI_D接続先をPD6→PD0に修正（ピンアサイン入れ替え反映）。§5「部品一覧（BOM）」新設。§6「外部接続・インターフェース」にエッジパッドとOLED仕様を統合。§7にClaude Code指示書PENDING明記。§8「設計根拠メモ」新設。§9「未決事項（PENDING）」に優先度追加・PENDING #5・#6を追加。目次追加。 |
